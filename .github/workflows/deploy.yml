name: Verify DevOps Container Infrastructure

on:
  push:
    branches:
      - main

jobs:
  verify-docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code Artifacts
        uses: actions/checkout@v4

      - name: Validate Dockerfile Build Mechanics
        run: docker build -t portfolio-site .